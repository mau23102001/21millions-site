/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No definimos headers aquí para NO duplicar la CSP que ya pone el middleware
};

export default nextConfig;
