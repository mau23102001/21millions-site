// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No definimos headers aquí para no duplicar CSP del middleware
};

export default nextConfig;
